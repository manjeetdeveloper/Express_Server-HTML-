# Express_Server-HTML

A simple Express.js server for serving static HTML pages. This project demonstrates how to set up a basic Node.js + Express server and serve static content (HTML, CSS, JS) for a lightweight web application.

## 📁 Project Structure


- `public/`: Contains all static files like HTML, CSS, and JS.
- `server.js`: Main server file that runs the Express app.
- `package.json`: Defines dependencies and scripts.

## 🚀 Features

- Serves static HTML files
- Lightweight and fast
- Easy to extend for dynamic routing or templating engines

## 🛠️ Installation

```bash
# Clone the repository
git clone https://github.com/manjeetdeveloper/Express_Server-HTML-.git

# Navigate into the directory
cd Express_Server-HTML-

# Install dependencies
npm install
